name: enrich-issues
on:
  schedule:
    - cron: "17 */6 * * *"   # every 6 hours (slower cron per spec §12 Phase 3)
  workflow_dispatch: {}
jobs:
  enrich:
    runs-on: ubuntu-latest
    concurrency:
      group: enrich-issues
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: "20" }
      - run: npm ci
      # Actions run fresh code against whatever schema the DB has; migrations
      # are idempotent, so apply them here too instead of racing the Render
      # deploy (which migrates on npm start).
      - run: npm run migrate
        env:
          GITHUB_TOKEN: ${{ secrets.GH_FEED_TOKEN }} # loadConfig requires it even for migrate
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
      - run: npm run enrich
        env:
          GITHUB_TOKEN: ${{ secrets.GH_FEED_TOKEN }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          ENRICH_MAX_ISSUES: "500"
          RETENTION_DAYS: "5"
